import statusArchived from '@images/status/status_archived.svg';
import statusCompleted from '@images/status/status_completed.svg';
import statusDelivered from '@images/status/status_delivered.svg';
import statusDraft from '@images/status/status_draft.svg';
import statusExpired from '@images/status/status_expired.svg';
import statusOpen from '@images/status/status_open.svg';
import statusPaired from '@images/status/status_paired.svg';
import statusPendingExport from '@images/status/status_pending_export.svg';
import statusProcessed from '@images/status/status_processed.svg';
import statusTimeout from '@images/status/status_timeout.svg';

export const statusSvgs = {
    archived: statusArchived,
    completed: statusCompleted,
    delivered: statusDelivered,
    draft: statusDraft,
    expired: statusExpired,
    open: statusOpen,
    paired: statusPaired,
    pending_export: statusPendingExport,
    processed: statusProcessed,
    timeout: statusTimeout,
};
